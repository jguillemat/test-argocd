apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: quay-ha-crds
resources:
  - namespace-quay-ha.yaml
secretGenerator:
- name: quay-config-quayha
  files:
  - config.yaml
  - clair-config.yaml
  - ./selfsigner-cert/ssl.cert
  - ./selfsigner-cert/ssl.key
  - ./selfsigner-cert/rootCA.pem

