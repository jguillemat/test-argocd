apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dns-operator
  namespace: openshift-gitops  
spec:
  project: default
  source:
    repoURL: git@github.com:jguillemat/test-argocd.git
    targetRevision: main    
    path: platform/dns-operator
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
