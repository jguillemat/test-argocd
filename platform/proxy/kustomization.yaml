apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: proxy
  annotations:
    # Wave 60 Configuración Cluster ( /platform ) 
    argocd.argoproj.io/sync-wave: "60"  
resources:
  - configmap-custom-ca-bundle.yaml
  - proxy-cluster.yaml
