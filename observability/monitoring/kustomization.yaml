apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: monitoring-config
  annotations:
    # Wave 80 Observabilidad  ( /observability ) 
    # Servicios de monitorización de la plataforma/aplicaciones 
    argocd.argoproj.io/sync-wave: "80"  
resources:
  - cluster-monitoring-configmap-no-storage.yaml
  - user-workload-monitoring-config-no-storage.yaml
