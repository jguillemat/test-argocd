apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: cluster-logging-operator
  annotations:
    # Wave 30 Operadores Base ( /operators ) 
    # Wave 35 Operadores Infraestructura ( /operators )     
    argocd.argoproj.io/sync-wave: "30" 
resources:
  - namespace-openshift-operators-redhat.yaml
  - namespace-openshift-logging.yaml
  - operatorgroup-cluster-logging.yaml
  - subs-cluster-logging.yaml

