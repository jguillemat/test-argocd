apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: cluster-logging-operator
  annotations:
    # Wave 20 Operadores Base ( /operators ) 
    # Operadores que proporcionan CRDs requeridos por otros componentes. 
    argocd.argoproj.io/sync-wave: "30"    
resources:
  - namespace-openshift-operators-redhat.yaml
  - namespace-openshift-logging.yaml
  - operatorgroup-cluster-logging.yaml
  - subscription-cluster-logging.yaml

